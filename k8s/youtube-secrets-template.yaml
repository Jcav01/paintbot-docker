# This is a template - DO NOT put real secrets here
# Use: kubectl create secret generic youtube-secrets --from-literal=youtube-api-key=... [--from-literal=webhook-secret=...]
apiVersion: v1
kind: Secret
metadata:
  name: youtube-secrets
type: Opaque
# Data is intentionally omitted; create with kubectl using from-literal or from-file.
